from typing import List, Dict


def find_similar(object_id: str, top_k: int = 20) -> List[Dict]:
    """Find similar objects using vector similarity"""
    # query vector store / pgvector; return similar ids + scores
    return []
